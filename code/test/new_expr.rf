use io
use string

fn add(a: int, b: int): int {
  return a + b;
}
fn pow(base: int, exp: int): int {
  result: int = 1;
  while exp > 0 {
    result = result * base;
    exp = exp - 1;
  };
  return result;
}
fn main(argc: int, argv: char**): int {
  if argc != 0 && argv {
    a: int = 0;
  };
  a: int = 4 + 3 * (x - 4);
  return x + 3 < 4 && a > 3;
}
fn func(): bool {
  return 1;
}
