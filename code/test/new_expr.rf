use io
use string

fn add(a: int*, b: int): int {
  sum: int = a + b;
  return sum;
}
fn pow(base: int, exp: int): int {
  return 1;
}
fn main(argc: int, argv: char**): int {
  affa: int = 4 + 3 * (x - 4);
  affa = (4 - 1) * 5;
  return x + 3 < 4 && a > 3;
}
fn func(a: int): bool {
  if a == 3 {
    a = 6;
  };
  while a < 10 {
    a = a - 1;
  };
  return 1;
}
