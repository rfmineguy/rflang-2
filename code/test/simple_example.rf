use io

fn add(a: int, b: int): int {
  return a + b;
};

fn main(): int {
  x: = 4 - 4 + 3 * 5;
  if x > 4 {
  };
  while x > 4 {
    if putc(43) && 3 || 1 {
      
    };
    x = x - 1;
  };
  return 4*3-add(63,add(4,1))/3;
};
