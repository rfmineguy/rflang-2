use io
use stdlib

fn add(a: int, b: int, c: short): int {
  sum: int = a + b + c;
  return sum;
}

fn sub(a: int, b: int): int {
  diff: int = a - b;
  return diff;
}

fn is_even(a: int): bool {
  res: bool = a % 2 == 0;
  return res;
}

fn main(argc: int, argv: char**): int {
  s: char* = "Hikagfjakhd\n";
  s2: char* = "fjk\tafhifoiafdafgag\r\n";
  s3: char* = "Hello World\n";
  val: int = (4 % 3) * 7 / (9 - 8);
  putc(54, 42);
  puts(s);
  return 9;
}
