use io
use stdlib

fn add(a: int, b: int, c: short): int {
  sum: int = a + b + c;
  sum = sum + 1;
  return sum;
}

fn sub(a: int, b: int): int {
  diff: int = a - b;
  return diff;
}

fn main(argc: int, argv: char**): int {
  s: char* = "Hikagfjakhd\n";
  s2: char* = "fjkafhifoiafdafgag\r\n";
  putc(54, 42);
  puts(s);
  return 9;
}
