fn pow(base: int, exp: int): int {
  r: int = 1;
  while exp > 0 {
    r = r * base;
  };
  return r;
};

fn imax(a: int, b: int): int {
  if a > b {
    return a;
  };
  return b;
};

fn imin(a: int, b: int): int {
  if a < b {
    return a;
  };
  return b;
};
